<svg viewBox="0 0 716 716" xmlns="http://www.w3.org/2000/svg">
  <style>
    /* Motion language: period ladder FAST 5s / MID 9s / SLOW 15s, stagger via
       -1.9s negative-delay steps only. The signal traveler is linear
       (continuous, exempt from the zero-velocity rule); port pulses and
       node-glow breathing use the sinusoidal token cubic-bezier(0.45,0,0.55,1)
       — 0%/100% keyframes identical, approached at zero velocity, so
       pause/resume mid-loop never jerks. */
    /* ALTERNATE-BRANCH ROUTING: the FAST tier is now a 10s COMBINED cycle of
       two 5s runs sharing one graph. Run 1 (0%-50% of 10s = 0-5s): the
       original comet takes the highlighted route via B (A→B→D→E). Run 2
       (50%-100% = 5-10s): a second comet takes the gray bypass via C
       (A→C→D→E). Each comet's offset-path/opacity keyframes are written on
       the FULL 10s timeline: visible+moving during its own half, held
       invisible during the other half, with matching values at the 0%/100%
       seam so the loop is a clean baton-pass — bypass comet completes at E
       exactly as the main comet reappears at A. See .wf-comet-main /
       .wf-comet-bypass below. */
    .flow {
      stroke-dasharray: 14 146;
      animation: flow 5s linear infinite;
    }
    @keyframes flow { from { stroke-dashoffset: 160; } to { stroke-dashoffset: 0; } }
    /* MID 9s: highlighted port dots pulse, staggered -1.9s steps */
    .pulse { animation: pulse 9s cubic-bezier(0.45, 0, 0.55, 1) infinite; }
    .pulse.d1 { animation-delay: -1.9s; }
    .pulse.d2 { animation-delay: -3.8s; }
    .pulse.d3 { animation-delay: -5.7s; }
    @keyframes pulse {
      0%, 100% { opacity: 0.33; }
      50%      { opacity: 0.6; }
    }
    /* SLOW 15s: every node panel exhales a faint accent halo, staggered -1.9s
       steps — reads as the whole system quietly alive even at rest */
    .node-breathe { animation: nodeBreathe 15s cubic-bezier(0.45, 0, 0.55, 1) infinite; opacity: 0.15; }
    .node-breathe.nb1 { animation-delay: -1.9s; }
    .node-breathe.nb2 { animation-delay: -3.8s; }
    .node-breathe.nb3 { animation-delay: -5.7s; }
    .node-breathe.nb4 { animation-delay: -7.6s; }
    @keyframes nodeBreathe {
      0%, 100% { opacity: 0.15; }
      50%      { opacity: 0.26; }
    }
    /* ROUTING: the two comets ride real, distinct paths (see math below),
       but both total L≈760.18 by construction — the bypass's A→C and C→D
       curves are exact mirrors of the main route's A→B and B→D curves (same
       control-point deltas, flipped vertically), C's box is the same width
       as B's, and both routes share the identical D→E curve as their last
       leg. That symmetry means each route's internal arrival fractions are
       the SAME (24.4% / 65.5% / 100% of ITS OWN 5s half) — only where that
       half sits on the shared 10s timeline differs.
       Main (run 1, local 0%-100% → master 0%-50%):
         seg1 A→B (L≈185.7) + through B's box (L=124) + seg2 B→D (L≈188.5)
         + through D's box via its upper port (420,346→544,358, L≈124.58)
         + seg3 D→E (L≈137.4). Total L≈760.18.
         → node B arrival:  master 0.5 × 24.4%  = 12.2%
         → node D arrival:  master 0.5 × 65.5%  = 32.75%
         → node E arrival:  master 0.5 × 100%   = 50%   (mid-cycle, NOT a seam)
       Bypass (run 2, local 0%-100% → master 50%-100%):
         seg1 A→C (L≈185.7, mirrors A→B) + through C's box (L=124)
         + seg2 C→D (L≈188.5, mirrors B→D) + through D's box via its LOWER
         port (420,370→544,358, L≈124.58, same magnitude as the main leg —
         both ports sit 12px from the exit y) + seg3 D→E (L≈137.4, the exact
         same curve the main route ends on — shared infrastructure).
         → node C arrival:  master 50 + 0.5×24.4% = 62.2%
         → node D arrival:  master 50 + 0.5×65.5% = 82.75%
         → node E arrival:  master 50 + 0.5×100%  = 100% ≡ 0%  (the true
           10s-cycle SEAM — this is why only E's bloom uses seam-wrapping). */
    /* Arrival blooms — anticipation → attack → decay per §18.5 (see
       cover-workflow.svg craft law: never narrower than 8% attack / 18%
       total envelope; a seam-crossing envelope carries the same peak value
       at both 0% and 100%, never hold-then-jump). Amplitudes per §18: halos
       swing 0.15→0.9. B and C each get ONE hump (their own route's single
       visit); D and E each get TWO — one per route — since both routes pass
       through them. */
    .node-arrive { animation-duration: 10s; animation-timing-function: cubic-bezier(0.45, 0, 0.55, 1); animation-iteration-count: infinite; }
    .arrive-b { animation-name: arriveB; }
    .arrive-c { animation-name: arriveC; }
    .arrive-d { animation-name: arriveD; }
    .arrive-e { animation-name: arriveE; }
    /* B (run 1 only): arrival 12.2% → attack begins 4.2% (8%), peak 12.2%,
       decay to 36.2% (24%) */
    @keyframes arriveB {
      0%    { opacity: 0.15; }
      4.2%  { opacity: 0.15; }
      12.2% { opacity: 0.9; }
      36.2% { opacity: 0.15; }
      100%  { opacity: 0.15; }
    }
    /* C (run 2 only, its ONLY appearance in the story): arrival 62.2% →
       attack begins 54.2% (8%), peak 62.2%, decay to 86.2% (24%) */
    @keyframes arriveC {
      0%    { opacity: 0.15; }
      54.2% { opacity: 0.15; }
      62.2% { opacity: 0.9; }
      86.2% { opacity: 0.15; }
      100%  { opacity: 0.15; }
    }
    /* D (BOTH halves): run-1 hump at 32.75% (attack 24.75%→32.75%, decay to
       56.75%); run-2 hump at 82.75% (attack 74.75%→82.75%, decay clipped to
       100% — 17.25% decay, still well past the 18% total-envelope floor —
       so the value lands back on the shared 0.15 baseline exactly at the
       seam, matching D's own 0% keyframe: no jump) */
    @keyframes arriveD {
      0%     { opacity: 0.15; }
      24.75% { opacity: 0.15; }
      32.75% { opacity: 0.9; }
      56.75% { opacity: 0.15; }
      74.75% { opacity: 0.15; }
      82.75% { opacity: 0.9; }
      100%   { opacity: 0.15; }
    }
    /* E (BOTH halves, terminal for both routes): run-1 hump is a plain
       mid-cycle bloom at 50% (attack 42%→50%, decay to 74% — NOT a seam,
       just an ordinary hump). Run-2 hump IS the true 10s seam (100%≡0%):
       §18.5 seam-wrapping — 0% and 100% carry the same peak (0.9), rise
       begins 92% (8% attack into the seam), decays 24% out the other side
       (down to 0.15 by 24%) before the flat plateau leading into run 1. */
    @keyframes arriveE {
      0%   { opacity: 0.9; }
      24%  { opacity: 0.15; }
      42%  { opacity: 0.15; }
      50%  { opacity: 0.9; }
      74%  { opacity: 0.15; }
      92%  { opacity: 0.15; }
      100% { opacity: 0.9; }
    }
    /* Terminal ring: unchanged shape, just retimed onto the new 10s cycle
       (duration 5s→10s) so its existing 0%/100%-seam-wrapped pre-glow +
       release still lands exactly on the run-2/bypass arrival at E — the
       true cycle seam. Pre-glow gathers before the seam, ring releases after
       arrival and expands wide (§18: ≥3× radius) over a fifth of the cycle. */
    .terminal-ping {
      transform-box: fill-box;
      transform-origin: center;
      animation: terminalPing 10s cubic-bezier(0.45, 0, 0.55, 1) infinite;
    }
    @keyframes terminalPing {
      0%   { opacity: 0.7; transform: scale(1.05); }
      19%  { opacity: 0; transform: scale(3.4); }
      20%  { opacity: 0; transform: scale(0.6); }
      88%  { opacity: 0; transform: scale(0.6); }
      92%  { opacity: 0.5; transform: scale(0.9); }
      100% { opacity: 0.7; transform: scale(1.05); }
    }
    /* THE SIGNAL IS A COMET — primary continuous motion, now TWO comets
       sharing the 10s cycle (see routing math above). Each carries its own
       real offset-path; each is hidden (opacity 0) for its off-half and
       fades in/out at its own half's boundary. The old single-comet dash
       fallback below stays as a low-opacity underlayer (for browsers
       without SVG offset-path support). */
    .wf-comet-main {
      offset-path: path("M170,358 C194,358 194,184 218,184 L342,184 C381,184 381,346 420,346 L544,358 C556,358 556,225 568,225");
      offset-rotate: 0deg;
      animation: wfCometMain 10s linear infinite;
    }
    @keyframes wfCometMain {
      0%     { offset-distance: 0%;   opacity: 1; }
      50%    { offset-distance: 100%; opacity: 1; }
      50.01% { opacity: 0; }
      100%   { opacity: 0; }
    }
    .wf-comet-bypass {
      offset-path: path("M170,358 C194,358 194,532 218,532 L342,532 C381,532 381,370 420,370 L544,358 C556,358 556,225 568,225");
      offset-rotate: 0deg;
      animation: wfCometBypass 10s linear infinite;
    }
    @keyframes wfCometBypass {
      0%     { opacity: 0; }
      49.99% { opacity: 0; }
      50%    { offset-distance: 0%;   opacity: 1; }
      100%   { offset-distance: 100%; opacity: 1; }
    }
    /* Gray bypass branch — while run 2's comet transits the two real gray
       edges (A→C, C→D), an accent overlay brightens them toward #1858ee,
       then returns to plain gray after (anticipation before 50%, sustained
       through the transit, decays once the comet reaches D — the rest of
       its journey, D→E, is already the permanently-blue shared trunk). Same
       envelope shape as the arrival blooms: 8% attack, seam-clean (0%/100%
       both 0, so nothing carries across the wrap). */
    .branch-glow-u, .branch-crisp { opacity: 0; }
    .branch-glow-u { animation: branchGlow 10s cubic-bezier(0.45, 0, 0.55, 1) infinite; }
    .branch-crisp  { animation: branchCrisp 10s cubic-bezier(0.45, 0, 0.55, 1) infinite; }
    @keyframes branchGlow {
      0%     { opacity: 0; }
      42%    { opacity: 0; }
      50%    { opacity: 0.6; }
      82.75% { opacity: 0.6; }
      100%   { opacity: 0; }
    }
    @keyframes branchCrisp {
      0%     { opacity: 0; }
      42%    { opacity: 0; }
      50%    { opacity: 1; }
      82.75% { opacity: 1; }
      100%   { opacity: 0; }
    }
    /* IN-NODE PROCESSING: a small accent fill bar per node scales in from
       width 0 (left-anchored) as that node's arrival attack rises, holds at
       full width, then fades out (opacity only — width stays full) to
       reveal the resting graphite/accent content bar beneath. Same anchors
       as the .arrive-* halos above so the flourish and the glow read as one
       event ("the node doing work"). B/C get one hump (their route's single
       visit); D/E get two (one per route). */
    .proc-bar {
      transform-box: fill-box;
      transform-origin: left center;
      opacity: 0;
      transform: scaleX(0);
      animation-duration: 10s;
      animation-timing-function: cubic-bezier(0.45, 0, 0.55, 1);
      animation-iteration-count: infinite;
    }
    .proc-b { animation-name: procB; }
    .proc-c { animation-name: procC; }
    .proc-d { animation-name: procD; }
    .proc-e { animation-name: procE; }
    @keyframes procB {
      0%    { transform: scaleX(0); opacity: 0; }
      4.2%  { transform: scaleX(0); opacity: 0; }
      12.2% { transform: scaleX(1); opacity: 1; }
      20%   { transform: scaleX(1); opacity: 1; }
      36.2% { transform: scaleX(1); opacity: 0; }
      100%  { transform: scaleX(1); opacity: 0; }
    }
    @keyframes procC {
      0%    { transform: scaleX(0); opacity: 0; }
      54.2% { transform: scaleX(0); opacity: 0; }
      62.2% { transform: scaleX(1); opacity: 1; }
      70%   { transform: scaleX(1); opacity: 1; }
      86.2% { transform: scaleX(1); opacity: 0; }
      100%  { transform: scaleX(1); opacity: 0; }
    }
    @keyframes procD {
      0%     { transform: scaleX(0); opacity: 0; }
      24.75% { transform: scaleX(0); opacity: 0; }
      32.75% { transform: scaleX(1); opacity: 1; }
      40%    { transform: scaleX(1); opacity: 1; }
      56.75% { transform: scaleX(1); opacity: 0; }
      74.75% { transform: scaleX(0); opacity: 0; }
      82.75% { transform: scaleX(1); opacity: 1; }
      90%    { transform: scaleX(1); opacity: 1; }
      100%   { transform: scaleX(1); opacity: 0; }
    }
    @keyframes procE {
      0%   { transform: scaleX(1); opacity: 1; }
      24%  { transform: scaleX(1); opacity: 0; }
      42%  { transform: scaleX(0); opacity: 0; }
      50%  { transform: scaleX(1); opacity: 1; }
      68%  { transform: scaleX(1); opacity: 1; }
      74%  { transform: scaleX(1); opacity: 0; }
      92%  { transform: scaleX(0); opacity: 0; }
      100% { transform: scaleX(1); opacity: 1; }
    }
    /* gentle whole-graph drift: every edge/node/comet/halo shares this ONE
       group transform, so edges stay attached to their nodes automatically —
       scale-free (translate only), SLOW 15s sine token, ±5px. */
    .wf-graph-drift {
      transform-origin: 358px 358px;
      animation: wfGraphDrift 15s cubic-bezier(0.45, 0, 0.55, 1) infinite;
    }
    @keyframes wfGraphDrift {
      0%, 100% { transform: translate(0px, 0px); }
      50%      { transform: translate(5px, -3px); }
    }
    @media (prefers-reduced-motion: reduce) {
      .flow, .pulse, .node-breathe, .node-arrive, .terminal-ping,
      .wf-comet-main, .wf-comet-bypass, .branch-glow-u, .branch-crisp,
      .proc-bar, .wf-graph-drift { animation: none; }
    }

    /* light ("blueprint on paper") palette — dark above stays default/unmodified */
    @media (prefers-color-scheme: light) {
      .lp-bg { fill: url(#bgL); }
      .lp-ambient { fill: url(#ambientGlowL); }
      .lp-dot { fill: #64748b; opacity: 0.10; }
      .lp-nf { fill: url(#nodeFillL); }
      .lp-node { fill: url(#nodeFillL); stroke: #b9c2cd; }
      .lp-line1 { stroke: #64748b; }
      .lp-bar[fill] { fill: #c3ccd6; }
      .lp-bar[stroke] { stroke: #c3ccd6; }
      .lp-glow-u { opacity: 0.20; }
      .port { fill: url(#nodeFillL); stroke: #64748b; }
      /* §15.5 item 5: brand-blue-on-brand-blue was invisible (dash #1858ee riding a
         line already stroked #1858ee) — near-white dash + wider stroke + a slight glow */
      .flow { stroke: #e8f0ff; stroke-width: 3; filter: url(#glow); }
      /* comet needs the same near-white treatment for the same reason —
         it also rides the #1858ee line (both main and bypass, once each
         reaches the shared D→E trunk / their own accent-brightened branch) */
      .wf-comet-blur { fill: #e8f0ff; opacity: 0.5; }
      .wf-comet-core { fill: #ffffff; }
      /* §15.5 item 5: pulsing port dots boosted for light visibility, 0.55-of-base floor */
      @keyframes pulse {
        0%, 100% { opacity: 0.33; }
        50%      { opacity: 0.6; }
      }
      /* §15.5 item 5: node-glow breathing boosted for light visibility, 0.55-of-base floor */
      @keyframes nodeBreathe {
        0%, 100% { opacity: 0.12; }
        50%      { opacity: 0.22; }
      }
    }
  </style>
  <defs>
    <radialGradient id="bg" cx="45%" cy="35%" r="78%">
      <stop offset="0%" stop-color="#1a1c20"/>
      <stop offset="55%" stop-color="#101114"/>
      <stop offset="100%" stop-color="#08090a"/>
    </radialGradient>
    <radialGradient id="bgL" cx="45%" cy="35%" r="78%">
      <stop offset="0%" stop-color="#ffffff"/>
      <stop offset="55%" stop-color="#f5f7fa"/>
      <stop offset="100%" stop-color="#eef1f5"/>
    </radialGradient>
    <radialGradient id="ambientGlow" cx="46%" cy="46%" r="42%">
      <stop offset="0%" stop-color="#1858ee" stop-opacity="0.16"/>
      <stop offset="60%" stop-color="#1858ee" stop-opacity="0.05"/>
      <stop offset="100%" stop-color="#1858ee" stop-opacity="0"/>
    </radialGradient>
    <radialGradient id="ambientGlowL" cx="46%" cy="46%" r="42%">
      <stop offset="0%" stop-color="#1858ee" stop-opacity="0.10"/>
      <stop offset="60%" stop-color="#1858ee" stop-opacity="0.035"/>
      <stop offset="100%" stop-color="#1858ee" stop-opacity="0"/>
    </radialGradient>
    <linearGradient id="nodeFill" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0%" stop-color="#202226"/>
      <stop offset="100%" stop-color="#121316"/>
    </linearGradient>
    <linearGradient id="nodeFillL" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0%" stop-color="#ffffff"/>
      <stop offset="100%" stop-color="#f2f4f7"/>
    </linearGradient>
    <pattern id="dots" width="40" height="40" patternUnits="userSpaceOnUse">
      <circle class="lp-dot" cx="2" cy="2" r="1" fill="#8a97a8" opacity="0.07"/>
    </pattern>
    <filter id="glow" x="-60%" y="-60%" width="220%" height="220%">
      <feGaussianBlur stdDeviation="6" result="blur"/>
      <feMerge>
        <feMergeNode in="blur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
    <filter id="dotGlow" x="-200%" y="-200%" width="500%" height="500%">
      <feGaussianBlur stdDeviation="4" result="blur"/>
      <feMerge>
        <feMergeNode in="blur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
  </defs>

  <rect class="lp-bg" width="716" height="716" fill="url(#bg)"/>
  <rect width="716" height="716" fill="url(#dots)"/>
  <rect class="lp-ambient" width="716" height="716" fill="url(#ambientGlow)"/>

  <!-- the whole graph (edges, nodes, comets, halos) is one drifting group
       so edges stay attached to their nodes for free -->
  <g class="wf-graph-drift">
  <!-- secondary (non-highlighted) branch, graphite hairline — this is the
       real gray bypass geometry the run-2 comet + accent overlay below ride -->
  <g class="lp-line1" fill="none" stroke="#48525f" stroke-width="2.5" stroke-linecap="round" opacity="0.85">
    <path d="M170,358 C194,358 194,532 218,532"/>
    <path d="M342,532 C381,532 381,370 420,370"/>
  </g>

  <!-- gray bypass branch: accent overlay brightens toward the accent while
       run 2's comet transits it, fades back to plain gray after (see
       .branch-glow-u / .branch-crisp above) -->
  <g class="branch-glow-u" fill="none" stroke="#1858ee" stroke-width="7" stroke-linecap="round" filter="url(#glow)">
    <path d="M170,358 C194,358 194,532 218,532"/>
    <path d="M342,532 C381,532 381,370 420,370"/>
  </g>
  <g class="branch-crisp" fill="none" stroke="#1858ee" stroke-width="2.5" stroke-linecap="round">
    <path d="M170,358 C194,358 194,532 218,532"/>
    <path d="M342,532 C381,532 381,370 420,370"/>
  </g>

  <!-- highlighted path: soft cyan glow underlay -->
  <g class="lp-glow-u wake-glow" fill="none" stroke="#22d3ee" stroke-width="7" stroke-linecap="round" opacity="0.30" filter="url(#glow)">
    <path d="M170,358 C194,358 194,184 218,184"/>
    <path d="M342,184 C381,184 381,346 420,346"/>
    <path d="M544,358 C556,358 556,225 568,225"/>
  </g>
  <!-- highlighted path: crisp brand-blue line -->
  <g fill="none" stroke="#1858ee" stroke-width="2.5" stroke-linecap="round">
    <path d="M170,358 C194,358 194,184 218,184"/>
    <path d="M342,184 C381,184 381,346 420,346"/>
    <path d="M544,358 C556,358 556,225 568,225"/>
  </g>
  <!-- dash-segment relay: low-opacity fallback layer (still real motion —
       kept for browsers without SVG offset-path support) now that the
       comets carry the primary signal. Covers all five real edges, main
       route and bypass alike, on its own unrelated 5s loop (it's ambient
       infrastructure texture, not route-gated). -->
  <g fill="none" stroke="#9ec5ff" stroke-width="2.5" stroke-linecap="round" opacity="0.16">
    <path class="flow" d="M170,358 C194,358 194,184 218,184"/>
    <path class="flow" style="animation-delay: -1.9s" d="M342,184 C381,184 381,346 420,346"/>
    <path class="flow" style="animation-delay: -3.8s" d="M544,358 C556,358 556,225 568,225"/>
    <path class="flow" style="animation-delay: -5.7s" d="M170,358 C194,358 194,532 218,532"/>
    <path class="flow" style="animation-delay: -7.6s" d="M342,532 C381,532 381,370 420,370"/>
  </g>

  <!-- ambient: node-glow breathing — a soft accent halo behind every panel, staggered,
       reads as the whole system quietly alive even at rest -->
  <g fill="none" stroke="#1858ee" filter="url(#glow)">
    <rect class="node-breathe nb0" x="43" y="327" width="130" height="62" rx="13" stroke-width="6"/>
    <rect class="node-breathe nb1" x="215" y="153" width="130" height="62" rx="13" stroke-width="6"/>
    <rect class="node-breathe nb2" x="215" y="501" width="130" height="62" rx="13" stroke-width="6"/>
    <rect class="node-breathe nb3" x="417" y="327" width="130" height="62" rx="13" stroke-width="6"/>
    <rect class="node-breathe nb4" x="565" y="197" width="102" height="56" rx="13" stroke-width="6"/>
  </g>

  <!-- signal-arrival blooms — flash brighter than the SLOW breathe above,
       exactly when a comet reaches that node (see keyframes). B is run-1
       only, C is run-2 only ("C joins the story only on its runs"), D and E
       bloom on BOTH runs. -->
  <g fill="none" stroke="#1858ee" filter="url(#glow)">
    <rect class="node-arrive arrive-b" x="215" y="153" width="130" height="62" rx="13" stroke-width="7"/>
    <rect class="node-arrive arrive-c" x="215" y="501" width="130" height="62" rx="13" stroke-width="7"/>
    <rect class="node-arrive arrive-d" x="417" y="327" width="130" height="62" rx="13" stroke-width="7"/>
    <rect class="node-arrive arrive-e" x="565" y="197" width="102" height="56" rx="13" stroke-width="8"/>
  </g>

  <!-- nodes -->
  <g>
    <rect class="lp-node" x="46" y="330" width="124" height="56" rx="11" fill="url(#nodeFill)" stroke="#4b5563" stroke-width="2"/>
    <rect class="lp-bar" x="50" y="334" width="116" height="1" fill="#7c8794" opacity="0.25"/>

    <rect class="lp-node" x="218" y="156" width="124" height="56" rx="11" fill="url(#nodeFill)" stroke="#4b5563" stroke-width="2"/>
    <rect class="lp-bar" x="222" y="160" width="116" height="1" fill="#7c8794" opacity="0.25"/>

    <rect class="lp-node" x="218" y="504" width="124" height="56" rx="11" fill="url(#nodeFill)" stroke="#4b5563" stroke-width="2"/>
    <rect class="lp-bar" x="222" y="508" width="116" height="1" fill="#7c8794" opacity="0.25"/>

    <rect class="lp-node" x="420" y="330" width="124" height="56" rx="11" fill="url(#nodeFill)" stroke="#4b5563" stroke-width="2"/>
    <rect class="lp-bar" x="424" y="334" width="116" height="1" fill="#7c8794" opacity="0.25"/>

    <rect class="lp-nf" x="568" y="200" width="96" height="50" rx="11" fill="url(#nodeFill)" stroke="#1858ee" stroke-width="2"/>
    <rect class="lp-bar" x="572" y="204" width="88" height="1" fill="#7c8794" opacity="0.25"/>
  </g>

  <!-- node content bars -->
  <g class="lp-bar" stroke="#3a4350" stroke-width="2" stroke-linecap="round" opacity="0.8">
    <line x1="64" y1="368" x2="118" y2="368"/>
    <line x1="236" y1="194" x2="300" y2="194"/>
    <line x1="236" y1="542" x2="300" y2="542"/>
    <line x1="438" y1="368" x2="502" y2="368"/>
  </g>
  <g stroke="#1858ee" stroke-width="2" stroke-linecap="round" opacity="0.7">
    <line x1="584" y1="235" x2="628" y2="235"/>
  </g>

  <!-- in-node processing: an accent fill bar scales in from the left on
       arrival, holds, fades to reveal the graphite/accent bar above — reads
       as the node doing work. Synced to each node's .arrive-* envelope. -->
  <g fill="#1858ee">
    <rect class="proc-bar proc-b" x="236" y="192.75" width="64" height="2.5" rx="1.25"/>
    <rect class="proc-bar proc-c" x="236" y="540.75" width="64" height="2.5" rx="1.25"/>
    <rect class="proc-bar proc-d" x="438" y="366.75" width="64" height="2.5" rx="1.25"/>
    <rect class="proc-bar proc-e" x="584" y="233.75" width="44" height="2.5" rx="1.25"/>
  </g>

  <!-- graphite port dots -->
  <g>
    <circle class="port" cx="218" cy="532" r="5" fill="#121316" stroke="#5b6675" stroke-width="2"/>
    <circle class="port" cx="342" cy="532" r="5" fill="#121316" stroke="#5b6675" stroke-width="2"/>
    <circle class="port" cx="420" cy="370" r="5" fill="#121316" stroke="#5b6675" stroke-width="2"/>
  </g>

  <!-- highlighted port dots: glow + crisp -->
  <g filter="url(#dotGlow)">
    <circle class="pulse" cx="170" cy="358" r="6" fill="#22d3ee" opacity="0.4"/>
    <circle class="pulse d1" cx="218" cy="184" r="6" fill="#22d3ee" opacity="0.4"/>
    <circle class="pulse d2" cx="342" cy="184" r="6" fill="#22d3ee" opacity="0.4"/>
    <circle class="pulse d3" cx="420" cy="346" r="6" fill="#22d3ee" opacity="0.4"/>
    <circle class="pulse d1" cx="544" cy="358" r="6" fill="#22d3ee" opacity="0.4"/>
    <circle class="pulse d2" cx="568" cy="225" r="6" fill="#22d3ee" opacity="0.4"/>
  </g>
  <g>
    <circle class="lp-nf" cx="170" cy="358" r="5" fill="#121316" stroke="#1858ee" stroke-width="2.5"/>
    <circle class="lp-nf" cx="218" cy="184" r="5" fill="#121316" stroke="#1858ee" stroke-width="2.5"/>
    <circle class="lp-nf" cx="342" cy="184" r="5" fill="#121316" stroke="#1858ee" stroke-width="2.5"/>
    <circle class="lp-nf" cx="420" cy="346" r="5" fill="#121316" stroke="#1858ee" stroke-width="2.5"/>
    <circle cx="544" cy="358" r="5" fill="#1858ee" stroke="#1858ee" stroke-width="2"/>
    <circle cx="568" cy="225" r="5.5" fill="#1858ee" stroke="#e6f0ff" stroke-width="1.5"/>
  </g>

  <!-- terminal port ping — expanding ring, opacity out, retimed to land on
       the run-2/bypass arrival at E (the true 10s-cycle seam) -->
  <circle class="terminal-ping" cx="568" cy="225" r="8" fill="none" stroke="#1858ee" stroke-width="2"/>

  <!-- THE SIGNAL IS A COMET — the primary continuous motion, now two of
       them sharing the 10s cycle. Blurred halo + crisp core traveling the
       real route via offset-path (see .wf-comet-main / .wf-comet-bypass).
       Drawn last so neither is ever occluded by a node as it crosses. -->
  <g class="wf-comet-main">
    <circle class="wf-comet-blur" r="10" fill="#9ec5ff" opacity="0.6" filter="url(#glow)"/>
    <circle class="wf-comet-core" r="3.5" fill="#eaf2ff"/>
  </g>
  <g class="wf-comet-bypass">
    <circle class="wf-comet-blur" r="10" fill="#9ec5ff" opacity="0.6" filter="url(#glow)"/>
    <circle class="wf-comet-core" r="3.5" fill="#eaf2ff"/>
  </g>
  </g>
</svg>
